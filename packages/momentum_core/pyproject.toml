[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "momentum-core"
version = "0.1.0"
description = "Core utilities for momentum trader"
authors = [
    {name = "Momentum Trader Team"}
]
dependencies = []
requires-python = ">=3.14"
readme = "README.md"

[dependency-groups]
dev = ["pytest", "ruff"]

[tool.setuptools.packages.find]
where = ["src"]
