# Configuration for Rainbow DQN Trading Agent Training

window_size: 60

logging:
  log_filename: training.log
  root_level: INFO
  console_level: INFO
  file_level: INFO
  level_overrides:
    Trainer: INFO
    Agent: INFO
    DataManager: INFO
    TransformerModel: INFO
    Buffer: INFO
    Metrics: INFO
    Evaluation: INFO

agent:
  # Core RL params
  gamma: 0.99           # Increased from 0.95; minute-scale trades still care about future bars
  lr: 0.0001            # Reduced from 3e-4; previous run showed policy churn from overshooting
  batch_size: 8192      # Reduced from 16384; better PER prioritization, still GPU-saturating
  replay_buffer_size: 1000000  # Doubled from 500k; more diverse replay with 24k training files
  target_update_freq: 1000     # Logging frequency only (Polyak updates every step regardless)

  # Network architecture
  window_size: 60
  n_features: 12
  hidden_dim: 256
  num_actions: 6  # Target allocation: 0%, 20%, 40%, 60%, 80%, 100%

  # Transformer params
  nhead: 4
  num_encoder_layers: 3
  dim_feedforward: 512
  transformer_dropout: 0.1

  # Rainbow params
  n_steps: 3            # Reduced from 5; shorter horizon for minute-scale momentum trades
  num_atoms: 101
  v_min: -1.0
  v_max: 1.0
  categorical_logging_interval: 2000
  categorical_logging_percentiles: [5, 25, 50, 75, 95]
  alpha: 0.6            # PER priority exponent
  beta_start: 0.4       # Increased from 0.3; less bias early on
  beta_frames: 200000   # Reduced from 400k; reach full IS correction sooner

  # LR scheduler
  lr_scheduler_enabled: true
  lr_scheduler_type: 'CosineAnnealingLR'  # Smoother than ReduceLROnPlateau for long runs
  lr_scheduler_params:
    T_max: 500000       # Cosine period in learner steps (~full training run)
    eta_min: 0.00001    # Floor LR

  # Exploration (epsilon-greedy on top of NoisyNets)
  epsilon_start: 0.3    # Initial random action probability; NoisyNets alone collapse to constant action
  epsilon_end: 0.05     # Floor epsilon after annealing (higher floor for long-run exploration)
  epsilon_decay_steps: 2000000  # Anneal over this many env steps (post-warmup)

  # Action entropy regularization
  entropy_coeff: 0.0    # Disabled; benchmark-relative reward structurally prevents action collapse

  # Other agent params
  grad_clip_norm: 1.0
  polyak_tau: 0.001     # Reduced from 0.005; more inertia in target network for stable bootstraps
  store_partial_n_step: true
  debug: false

environment:
  window_size: 60
  initial_balance: 1000.0
  transaction_fee: 0.001
  reward_scale: 1.0         # Per-step rewards O(1e-4 to 1e-3); n-step returns well within C51 support [-1, 1]
  invalid_action_penalty: -0.1   # Slightly stronger signal for masked-but-still-penalized actions
  drawdown_penalty_lambda: 0.3   # Midpoint: 0.5 caused death spirals, 0.15 caused action collapse to 100% exposure
  slippage_bps: 5.0
  opportunity_cost_lambda: 0.0   # Disabled; subsumed by benchmark-relative reward
  benchmark_allocation_frac: 0.5  # Reward = excess return over this fixed allocation; prevents action collapse
  min_rebalance_pct: 0.02      # Minimum allocation delta to trigger a trade (2% of portfolio)
  min_trade_value: 1.0         # Minimum trade notional in USD (Alpaca crypto minimum)

# Data processing params (used by preprocess_npz.py, not passed to TradingEnvConfig)
feature_rolling_window: 20   # Lookback bars for derived features (realized vol, volume ratio, HL range)

trainer:
  seed: 42
  warmup_steps: 20000       # With action masking, warmup produces cleaner transitions
  update_freq: 20           # Learn every 20 env steps (~20 min of bars); more diverse recent experience
  gradient_updates_per_step: 1  # Reduced from 3; lower effective step size to prevent policy churn
  log_freq: 60
  per_stats_log_freq: 0
  validation_freq: 200      # Less frequent validation; avoids judging a still-learning policy too often
  checkpoint_save_freq: 50
  reward_window: 20
  reward_clip: 0.5          # Clip to match C51 support [-1, 1]; per-step rewards are O(1e-4) to O(1e-3)
  min_episodes_before_early_stopping: 5000  # Defer best-tracking and early stopping until meaningful training
  early_stopping_patience: 12  # Increased from 6; more runway for slow convergence
  final_phase_lr_start_frac: 0.85  # Later start since cosine already decays smoothly
  final_phase_lr_multiplier: 0.3
  min_validation_threshold: 0.001

# --- Run Configuration --- #
run:
  mode: 'train'
  episodes: 50000
  model_dir: 'models'
  resume: false
  specific_file: null
  skip_evaluation: false
